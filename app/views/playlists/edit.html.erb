<div class="container">
  <div class="row">
    <div class="col d-flex align-items-center">
      <span class="back-button"><%= link_to "<", playlists_path %></span>
      <h1><%= @playlist.name %></h1>
    </div>
  </div>
</div>

<%= render 'form' %>

<div class="container">
  <div class="row">
    <div class="col">
      <%= link_to "Edit this playlist (not working)", edit_playlist_path(@playlist), class: "btn btn-primary btn-block" %>
    </div>
  </div>
  <div class="row">
    <div class="col">
      <%= link_to "Delete this playlist (not working)", playlist_path(@playlist), method: :delete, data: {confirm: "Are you sure ?"}, class: "btn btn-danger btn-block" %>
    </div>
  </div>
</div>
